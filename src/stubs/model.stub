<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
{{ use_statements }}
class {{ model }} extends Model
{{{ traits }}{{ primary_key }}

    protected $fillable = [
        {{ fillable }}
    ];

    protected $casts = [{{ casts }}]; {{ setter }} {{ getter }} {{ relations }}
}
